{
	"name": "@mindbuddy/ui",
	"version": "1.0.0",
	"description": "",
	"main": "src/index.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "echo \"UI package doesn't need compilation, it's directly imported\" && exit 0"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"packageManager": "pnpm@10.11.0",
	"dependencies": {
		"@mindbuddy/core": "workspace:*"
	}
}
