// This is a temporary initialization file that will be replaced
// when the core package is fully built with the modular storage system

/**
 * Initialize the MindBuddy storage system for web
 * This should be called at application startup
 */
export function initMindBuddy() {
  // Later, this will initialize the storage system
  console.log('MindBuddy initializing with direct localStorage');
}

// Initialize automatically
initMindBuddy(); 